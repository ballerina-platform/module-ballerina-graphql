query { 
    backend: languages {
        name(key: "backend")
    }
    frontend: languages {
        name(key: "frontend")
    }
    data: languages {
        name(key: "data")
    }
    native: languages {
        name(key: "native")
    },
    backend1: updatedLanguages {
        name(key: "backend")
    }
    frontend1: updatedLanguages {
        name(key: "frontend")
    }
    data1: updatedLanguages {
        name(key: "data")
    }
    native1: updatedLanguages {
        name(key: "native")
    },
}
