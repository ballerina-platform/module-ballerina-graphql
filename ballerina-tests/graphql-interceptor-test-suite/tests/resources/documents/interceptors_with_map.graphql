query {
  languages {
    backend: name(key: "backend")
    name(key: "frontend")
    data: name(key: "data")
    native: name(key: "native")
  }
  updatedLanguages {
    backend: name(key: "backend")
    frontend: name(key: "frontend")
    data: name(key: "data")
    native: name(key: "native")
  }
}
