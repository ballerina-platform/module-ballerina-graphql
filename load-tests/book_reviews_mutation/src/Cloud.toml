[container.image]
repository = "ballerina"
name = "book_reviews_mutation"

[cloud.deployment]
min_memory = "256Mi"
max_memory = "2048Mi"
min_cpu = "200m"
max_cpu = "1000m"

[cloud.deployment.autoscaling]
min_replicas = 1
max_replicas = 1
