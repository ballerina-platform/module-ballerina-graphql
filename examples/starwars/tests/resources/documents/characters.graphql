query {
    characters(idList: ["1000", "2000"]) {
        ...on Human {
            name
            homePlanet
        }
        ...on Droid {
            name
            primaryFunction
        }
    }
}
